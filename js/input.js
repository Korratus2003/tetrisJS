function handleInput() {
    document.addEventListener('keydown', (event) => {
        switch (event.key) {
            case 'ArrowLeft':
                // Ruch w lewo
                break;
            case 'ArrowRight':
                // Ruch w prawo
                break;
            case 'ArrowDown':
                // Ruch w dół
                break;
            case 'ArrowUp':
                // Obrót
                break;
        }
    });
}
